{
  "name": "kinetic-sports-nutrition",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n backend,storefront -c blue,green \"npm run dev -w apps/backend\" \"npm run dev -w apps/storefront\"",
    "dev:backend": "npm run dev -w apps/backend",
    "dev:storefront": "npm run dev -w apps/storefront",
    "migrate": "npm run db:migrate -w apps/backend",
    "build": "npm run build -w apps/backend && npm run build -w apps/storefront",
    "lint": "npm run lint -w apps/storefront"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
